# 1 : an object of class 'trellis'

# 2 : a set of 3 panels showing relationship between weight and time for each diet

# 3 : panel.lmline(); lpoints()

# 4 : the object p has not yet been printed

# 5 trellis.par.set()

# 6 the grammar of graphics developed by Leland Wilkinson

# 7 : airquality = transform(airquality, Month = factor(Month))
# qplot....

# 8 a ploting object like point,...

# 9 : g <- ggplot(movies, aes(votes, rating)). D'ou ggplot does not yet know what type of layer

# 10 : qplot(votes, rating, data = movies) + geom_smooth()